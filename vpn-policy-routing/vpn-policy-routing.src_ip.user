#!/bin/sh

SOURCE_IPSET='wan2'
SOURCE_FNAME='192.168.10.0/24 192.168.11.0/24'

for entry in $SOURCE_FNAME; do
	if ! ipset test $SOURCE_IPSET $entry; then
		ipset add $SOURCE_IPSET $entry
	fi
done

iptables -t mangle -A PREROUTING -m set --match-set $SOURCE_IPSET src -j MARK --set-mark 0x1

exit 0
